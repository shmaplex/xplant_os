---
title: "API overview"
description: "Every v1 endpoint, the scope it needs, and what it returns."
---

{/* Generated by scripts/generate-api-pages.mjs from openapi/openapi.json. Do not edit by hand. */}

Base URL: `https://app.xplantpro.com/api/v1`. Send your key as `Authorization: Bearer <key>` on every request. Request and response bodies are JSON.

Every response uses the same envelope:

```json
{ "ok": true, "data": { } }
{ "ok": false, "data": null, "error": "Missing scope: write:tasks", "code": "FORBIDDEN" }
```

Read `data` on success. On failure, branch on `code`, which is stable; `error` is for people and its wording can change. See [Errors](/docs/errors).

The machine-readable spec is [openapi.json](/openapi.json) (OpenAPI 3).

## Account

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`GET /me`](/docs/api/account/get-me) | Get the calling key | none |
| [`GET /workspaces`](/docs/api/account/list-workspaces) | Get the workspace | `read:workspace` |

## Plants

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`GET /plants`](/docs/api/plants/list-plants) | List plants | `read:plants` |
| [`GET /plants/{id}`](/docs/api/plants/get-plant) | Get a plant | `read:plants` |

## Explants

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`GET /explants`](/docs/api/explants/list-explants) | List explants | `read:explants` |
| [`GET /explants/{id}`](/docs/api/explants/get-explant) | Get an explant | `read:explants` |

## Transfers and stages

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`GET /stages`](/docs/api/transfers-and-stages/list-stages) | List stages | `read:transfers` |
| [`POST /stages`](/docs/api/transfers-and-stages/create-stage) | Advance a stage | `write:transfers` |
| [`GET /transfers`](/docs/api/transfers-and-stages/list-transfers) | List transfers | `read:transfers` |
| [`POST /transfers`](/docs/api/transfers-and-stages/create-transfer) | Record a transfer | `write:transfers` |

## Change history

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`GET /events`](/docs/api/change-history/list-change-events) | List change history | `read:events` |

## Tasks and demand

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`GET /tasks`](/docs/api/tasks/list-tasks) | List tasks | `read:tasks` |
| [`POST /tasks`](/docs/api/tasks/create-task) | Create a task | `write:tasks` |
| [`GET /tasks/{id}`](/docs/api/tasks/get-task) | Get a task | `read:tasks` |
| [`PATCH /tasks/{id}`](/docs/api/tasks/update-task) | Update a task | `write:tasks` |
| [`GET /tasks/demand`](/docs/api/tasks/list-demand-signals) | List demand signals | `read:tasks` |
| [`POST /tasks/demand`](/docs/api/tasks/create-demand-signal) | Record a demand signal | `write:demand` |

## SOPs and runs

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`POST /sop-runs`](/docs/api/sops/create-sop-run) | Start an SOP run | `write:sop_runs` |
| [`GET /sop-runs/{id}`](/docs/api/sops/get-sop-run) | Get an SOP run | `read:sop_runs` |
| [`POST /sop-runs/{id}/steps/{stepId}/events`](/docs/api/sops/create-sop-step-event) | Record step evidence | `write:sop_steps` |
| [`POST /sop-runs/{id}/steps/{stepId}/measurements`](/docs/api/sops/create-sop-step-measurement) | Record a step measurement | `write:sop_steps` |
| [`GET /sops`](/docs/api/sops/list-sops) | List SOPs | `read:sops` |
| [`GET /sops/{id}`](/docs/api/sops/get-sop) | Get an SOP | `read:sops` |

## Labels

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`POST /label-scans`](/docs/api/labels/create-label-scan) | Record a label scan | `write:label_scans` |
| [`GET /labels/resolve`](/docs/api/labels/resolve-label) | Resolve a label | `read:labels` |

## Devices

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`POST /device-events`](/docs/api/devices/create-device-event) | Record a device event | `write:device_events` |
| [`GET /devices`](/docs/api/devices/list-devices) | List devices | `read:devices` |
| [`POST /devices`](/docs/api/devices/create-device) | Register a device | `write:devices` |
| [`POST /devices/{deviceId}/heartbeat`](/docs/api/devices/send-heartbeat) | Send a heartbeat | `write:devices` |
| [`GET /devices/{deviceId}/tokens`](/docs/api/devices/list-device-tokens) | List device tokens | `read:devices` |
| [`POST /devices/{deviceId}/tokens`](/docs/api/devices/create-device-token) | Create a device token | `write:devices` |
| [`DELETE /devices/{deviceId}/tokens/{tokenId}`](/docs/api/devices/revoke-device-token) | Revoke a device token | `write:devices` |

## Sensor readings

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`GET /sensor-readings`](/docs/api/sensor-readings/list-sensor-readings) | List sensor readings | `read:sensor_readings` |
| [`POST /sensor-readings`](/docs/api/sensor-readings/create-sensor-readings) | Submit sensor readings | `write:sensor_readings` |

## Equipment

| Endpoint | What it does | Scope |
| --- | --- | --- |
| [`POST /equipment/{id}/events`](/docs/api/equipment/create-equipment-event) | Record an equipment event | `write:equipment_events` |
