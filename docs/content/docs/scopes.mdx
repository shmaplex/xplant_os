---
title: "Scopes"
description: "What each of the 34 scopes allows, and the endpoints it unlocks."
---

{/* Generated by scripts/generate-api-pages.mjs from openapi/openapi.json. Do not edit by hand. */}

A key carries exactly the scopes you choose when you create it in [Settings → Integrations → API Keys](https://app.xplantpro.com/settings/integrations/api-keys). There is no default set, and no scope implies another: `write:tasks` does not grant `read:tasks`.

**Scopes are fixed when a key is created.** To change what an integration can do, create a new key with the scopes it needs, move the integration to it, then revoke the old key.

Scope names follow `<read|write>:<resource>`. Grant each integration only what it calls.

A request without the scope it needs gets `403 FORBIDDEN`, and the message names the missing scope, for example `Missing scope: write:tasks`. To see what a key holds before you call anything, use [`GET /me`](/docs/api/account/get-me); it needs no scope.

Device tokens (`xpd_`) don't carry scopes. They can only write readings, heartbeats and events for their own device. See [Device tokens](/docs/device-tokens).

## Common setups

| Integration | Scopes |
| --- | --- |
| Read-only dashboard | `read:workspace`, `read:plants`, `read:explants`, `read:sensor_readings` |
| Task sync from your scheduler | `read:tasks`, `write:tasks`, `write:demand` |
| Bench station running SOPs | `read:sops`, `write:sop_runs`, `read:sop_runs`, `write:sop_steps`, `read:labels`, `write:label_scans` |
| Transfer and stage logging | `read:plants`, `read:explants`, `read:transfers`, `write:transfers` |
| Device provisioning (run once, off the device) | `read:devices`, `write:devices` |
| Mirror change history into your warehouse | `read:events`, `read:plants`, `read:explants` |

## All scopes

### Workspace

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:workspace` | Read workspace and lab settings | [`GET /workspaces`](/docs/api/account/list-workspaces) |

### Plants

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:plants` | Read plant records | [`GET /plants`](/docs/api/plants/list-plants)<br />[`GET /plants/{id}`](/docs/api/plants/get-plant) |
| `write:plants` | Create and update plant records | No endpoint yet |

### Explants

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:explants` | Read explant records | [`GET /explants`](/docs/api/explants/list-explants)<br />[`GET /explants/{id}`](/docs/api/explants/get-explant) |
| `write:explants` | Create and update explant records | No endpoint yet |

### Contaminations

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:contaminations` | Read contamination logs | No endpoint yet |
| `write:contaminations` | Submit contamination observations | No endpoint yet |

### Tasks

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:tasks` | Read scheduled tasks and due work | [`GET /tasks`](/docs/api/tasks/list-tasks)<br />[`GET /tasks/{id}`](/docs/api/tasks/get-task)<br />[`GET /tasks/demand`](/docs/api/tasks/list-demand-signals) |
| `write:tasks` | Create, update, complete, and reopen tasks | [`POST /tasks`](/docs/api/tasks/create-task)<br />[`PATCH /tasks/{id}`](/docs/api/tasks/update-task) |
| `write:demand` | Push sales/order demand numbers per genus for task prioritization | [`POST /tasks/demand`](/docs/api/tasks/create-demand-signal) |

### Comments

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:comments` | Read notes and comments on lab records | No endpoint yet |
| `write:comments` | Add notes and comments to lab records | No endpoint yet |

### Media

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:assets` | Read photos and media attached to lab records | No endpoint yet |
| `write:assets` | Attach photos and media to lab records | No endpoint yet |
| `read:media_recipes` | Read media recipe data | No endpoint yet |
| `write:media_recipes` | Create and update media recipes | No endpoint yet |

### Transfers

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:transfers` | Read transfer and stage history for plants and explants | [`GET /stages`](/docs/api/transfers-and-stages/list-stages)<br />[`GET /transfers`](/docs/api/transfers-and-stages/list-transfers) |
| `write:transfers` | Record transfers and advance the stage of plants and explants | [`POST /stages`](/docs/api/transfers-and-stages/create-stage)<br />[`POST /transfers`](/docs/api/transfers-and-stages/create-transfer) |

### SOPs

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:sops` | Read SOP templates and steps | [`GET /sops`](/docs/api/sops/list-sops)<br />[`GET /sops/{id}`](/docs/api/sops/get-sop) |
| `read:sop_runs` | Read SOP execution history: who ran which step, when | [`GET /sop-runs/{id}`](/docs/api/sops/get-sop-run) |
| `write:sop_runs` | Create and advance SOP run sessions | [`POST /sop-runs`](/docs/api/sops/create-sop-run) |
| `write:sop_steps` | Post confirmations, scans and measurements against a step of an SOP run | [`POST /sop-runs/{id}/steps/{stepId}/events`](/docs/api/sops/create-sop-step-event)<br />[`POST /sop-runs/{id}/steps/{stepId}/measurements`](/docs/api/sops/create-sop-step-measurement) |

### Labels

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:labels` | Resolve QR/barcode label codes to entity records | [`GET /labels/resolve`](/docs/api/labels/resolve-label) |
| `write:label_scans` | Submit label scan events | [`POST /label-scans`](/docs/api/labels/create-label-scan) |

### Devices

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:devices` | List registered devices and their status | [`GET /devices`](/docs/api/devices/list-devices)<br />[`GET /devices/{deviceId}/tokens`](/docs/api/devices/list-device-tokens) |
| `write:devices` | Register devices, send heartbeats, and manage device tokens | [`POST /devices`](/docs/api/devices/create-device)<br />[`POST /devices/{deviceId}/heartbeat`](/docs/api/devices/send-heartbeat)<br />[`POST /devices/{deviceId}/tokens`](/docs/api/devices/create-device-token)<br />[`DELETE /devices/{deviceId}/tokens/{tokenId}`](/docs/api/devices/revoke-device-token) |
| `read:sensor_readings` | Query historical sensor readings | [`GET /sensor-readings`](/docs/api/sensor-readings/list-sensor-readings) |
| `write:sensor_readings` | Submit environmental sensor readings (temperature, humidity, etc.) | [`POST /sensor-readings`](/docs/api/sensor-readings/create-sensor-readings) |
| `write:device_events` | Submit device status events | [`POST /device-events`](/docs/api/devices/create-device-event) |

### Equipment

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:equipment` | Read the lab equipment library and its maintenance history | No endpoint yet |
| `write:equipment_events` | Record that a piece of equipment was used, calibrated, serviced or faulted | [`POST /equipment/{id}/events`](/docs/api/equipment/create-equipment-event) |

### Commercial

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:pricing` | Read culture line prices, pricing tiers, and price history | No endpoint yet |
| `read:commerce` | Read store order lines and sell-through summaries | No endpoint yet |

### History

| Scope | Allows | Endpoints |
| --- | --- | --- |
| `read:events` | Read plant and explant edit history (audit events) | [`GET /events`](/docs/api/change-history/list-change-events) |

### No scope needed

- [`GET /me`](/docs/api/account/get-me): Get the calling key

Scopes marked “No endpoint yet” can already be granted to a key, and endpoints that use them are on the way. This page updates as they ship.
